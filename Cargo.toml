# GENERATED by heso-enterprise/scripts/sync-oss.sh — do not edit in the mirror.
# 0.4.2 is stamped from the enterprise workspace version at sync time.
[workspace]
resolver = "2"
members = [
    "crates/heso-core",
    "crates/heso-verify",
    "crates/heso-action",
    "crates/heso-verify-cli",
]

[workspace.package]
version = "0.4.2"
edition = "2021"
rust-version = "1.90"
license = "MIT OR Apache-2.0"
repository = "https://github.com/heso-inc/heso"
authors = ["Akshay and HESO contributors"]

[workspace.dependencies]
heso-verify = { path = "crates/heso-verify" } # RFC 8785 canonicalization + BLAKE3 + Ed25519 verify_strict
heso-core = { path = "crates/heso-core" }     # IdentityKey — Ed25519 seed-on-disk signer

# Pinned once, mirroring the enterprise workspace so a mirror build resolves
# the same crate versions the published artifacts were built with.
thiserror = "2.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_jcs = "0.2"
url = { version = "2", features = ["serde"] }
blake3 = "1"
ed25519-dalek = { version = "2.2", default-features = false, features = ["std", "rand_core"] }
rand_core = { version = "0.6", default-features = false, features = ["std", "getrandom"] }
base64 = { version = "0.22", default-features = false, features = ["std"] }
aes-gcm = "0.10"
argon2 = "0.5"
zeroize = { version = "1", features = ["zeroize_derive"] }
